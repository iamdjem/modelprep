{
  "name": "modelprep-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker backend for ModelPrep — Cults3D first.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240924.0",
    "typescript": "5.9.3",
    "wrangler": "^4.0.0"
  }
}
